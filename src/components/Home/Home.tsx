import classes from './Home.module.scss';

const Home = () => {
  return (
    <div className={`${classes.container} ${classes.homePage}`}>
      <div>
        <h1>Hi Hello</h1>
      </div>
    </div>
  );
};

export default Home;
